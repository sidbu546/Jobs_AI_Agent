# Deployment requirements — what the Streamlit app process actually imports.
# pyproject.toml remains the full dev/spec list (incl. langgraph for the
# orchestrator pipeline, playwright for future ATS auto-fill, pytest, etc.);
# those are deliberately left out here to keep Hugging Face Spaces builds
# small and fast.

streamlit>=1.36.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Storage
sqlalchemy>=2.0.0
chromadb>=0.5.0

# Job ingestion
httpx>=0.27.0
tenacity>=8.5.0

# Matching (torch is pulled in transitively by sentence-transformers)
sentence-transformers>=3.0.0
numpy

# LLM + MCP
anthropic>=0.30.0
mcp>=1.2.0
